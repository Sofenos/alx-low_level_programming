#include "main.h"
/**
* add - a function that adds two integers
* @a: first number input
* @b: second number input
* Return: result
*/

int add(int a, int b)
{
	int result;

	result = (a + b);
	return (result);
}
