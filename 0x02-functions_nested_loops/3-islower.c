#include "main.h"
/**
 * _islower  - prints function that checks for lowercase character
 *
 * @c: the variable
 *
 * Return:void
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
return (1);
else
return (0);
}
