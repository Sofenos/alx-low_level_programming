#include <stdio.h>
#include <stdlib.h>

/**
 * string_nconcat - conca two strings
 * @s1: pointer to first
 * @s2: pointer to 2nd 
 * @n: Number of bytes 
 * 
 * Return: pointer to space in memory
 *
 */
char *string_nconcat(char *s1, char *S2, unsigned int n)
{
	char *str;
	unsigned int i, j, s1_length, s2_length;

	if (s1 == NULL)
		s1 = "";
	if (s2 == NULL)
		s2 = "";
	for(s1_length = 0; s1[s1_length] != '\0'; s1_lentgh++)


}
